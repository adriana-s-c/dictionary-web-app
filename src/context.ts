import { createContext } from "react";

export const WordContext = createContext<any | undefined>([]);

export const DictionaryDefitinitionContext = createContext<any | undefined>([]);

export const FontContext = createContext<any | undefined>([]);
